# Docker

Por ahora Docker se usara unicamente para levantar PostgreSQL durante el desarrollo.

El backend todavia se ejecuta localmente con:

```bash
npm run dev
```

Mas adelante se podra agregar una configuracion para dockerizar tambien el backend.
